export { GAME_STATES, NUM_CARDS };

const GAME_STATES = {
  ERROR: "error",
  LOADING: "loading",
  READY: "ready",
  IN_PROGRESS: "in-progress",
  GAME_OVER_LOSS: "game-over-loss",
  GAME_OVER_WIN: "game-over-win",
};

const NUM_CARDS = 12;